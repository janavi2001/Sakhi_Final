name: sakhi
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.3
  date_format: ^2.0.2

  #Firebase
  firebase_core: ^1.2.1
  firebase_auth: ^1.3.0
  cloud_firestore: ^1.0.1
  intl: ^0.17.0

  provider: ^5.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter_icons:
  android: true
  ios: true
  image_path: "assets/images/logo.png" 

flutter:
  uses-material-design: true

  assets:
    - assets/images/logo.png
    - assets/images/avatar.png
  #C:\Users\janav\OneDrive\Desktop\Projects\flutter_firebase_auth_tutorial\assets\logo.jpeg
